# Backend environment variables (example)
#
# Ollama (local)
OLLAMA_MODEL=llama3:latest
OLLAMA_NUM_GPU=0
#
# Gemini (optional, cloud)
# DO NOT commit your real key in git. Keep it local.
GEMINI_API_KEY=your_key_here

# Supabase / Postgres (recommended: use the pooler, add sslmode=require)
DATABASE_URL=postgresql://postgres.<project-ref>:YOUR_PASSWORD@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require

# Admin endpoints (Insights)
# IMPORTANT: keep it secret (do not commit a real token).
ADMIN_TOKEN=change_me

# Supabase Storage (Ambiances cloud)
# Crée un bucket privé (ex: bn3-audio) et mets tes fichiers aux chemins:
# - music/user/slowlife.mp3, slowmotion.mp3, yesterday.mp3, dawnofchange.mp3
# - ambiences/rain.mp3, forest.mp3, ocean.mp3, wind.mp3, fire.mp3, pink-noise.mp3
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
SUPABASE_STORAGE_BUCKET=bn3-audio
SUPABASE_SIGNED_URL_EXPIRES=3600
SUPABASE_CATALOG_CACHE_SECONDS=30

