# Backend environment variables (example)
#
# Ollama (local)
OLLAMA_MODEL=llama3:latest
OLLAMA_NUM_GPU=0
#
# Gemini (optional, cloud)
# DO NOT commit your real key in git. Keep it local.
GEMINI_API_KEY=your_key_here

# Supabase / Postgres (recommended: use the pooler, add sslmode=require)
DATABASE_URL=postgresql://postgres.<project-ref>:YOUR_PASSWORD@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require

# Admin endpoints (Insights)
# IMPORTANT: keep it secret (do not commit a real token).
ADMIN_TOKEN=change_me

