fastapi==0.115.2
uvicorn==0.30.1
httpx==0.27.0
# NOTE: Render may use newer Python (e.g. 3.13). Numpy 1.26.x doesn't ship wheels for newer Pythons.
# Use a newer Numpy that has prebuilt wheels on modern Python runtimes.
numpy==2.2.1
pydantic==2.8.2
pyttsx3==2.90
psycopg[binary]==3.1.19
python-dotenv==1.0.1

